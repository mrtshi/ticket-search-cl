# Polair — переменные окружения

# Application
PORT=8080
NODE_ENV=development

# DynamoDB (Yandex Cloud Document API)
DOCUMENT_API_ENDPOINT=
DOCUMENT_API_REGION=ru-central1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
USE_DATABASE=false

# Google Sheets CSV (опубликованная таблица)
# Ссылка задаётся в lib/sheet-config.ts (поле csvUrl)